import { cameraType } from "./camera";
import { lensType } from "./lens";
import { adapterType } from "./adapter";
import { mountType } from "./mount";
import { compatibilityRuleType } from "./compatibilityRule";
import { sourceType } from "./source";

export const schemaTypes = [
  cameraType,
  lensType,
  adapterType,
  mountType,
  compatibilityRuleType,
  sourceType,
];
